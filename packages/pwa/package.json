{
  "name": "@claude-notifier/pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@claude-notifier/shared": "workspace:*",
    "qr-scanner": "^1.4.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vite": "^5.4.11",
    "wrangler": "^3.99.0"
  }
}
